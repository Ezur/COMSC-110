#include <iostream>
using namespace std;

int getProduct(int, int);

int main()
{
    int num1 = 5, num2 = 10, product;
    return product = getProduct(num1, num2);
}

int getProduct(int v1, int v2)
{
    int prod;
    return prod = v1 * v2;
}
